# 🧠 Simplify RAG: DeepSeek 学术论文助手

> 一个基于 **DeepSeek V3** 和 **RAG (检索增强生成)** 技术的本地论文阅读系统。
> 专为解决长文档阅读困难、双栏论文排版混乱、语言障碍而设计。

---

## ✨ 核心亮点 (Key Features)

### 1. 🧬 智能版面分析 (Layout Analysis)
解决了传统 PDF 读取工具（如 PyPDF2）在处理**双栏学术论文**时，文字顺序错乱、单词粘连的问题。
- **技术实现**：使用 `pdfplumber` 的 `layout=True` 模式 + 正则表达式清洗管道。

### 2. 🌍 跨语言检索 (Cross-Lingual Search)
打破语言障碍，支持**用中文直接提问全英文论文**。
- **技术实现**：集成 HuggingFace 的 `paraphrase-multilingual-MiniLM-L12-v2` 模型，实现语义级的跨语言对齐。

### 3. 🔍 精准溯源与防幻觉 (Anti-Hallucination)
拒绝 AI 瞎编。每一次回答都必须基于文档事实。
- **功能**：内置 **Debug 模式**（侧边栏），实时展示 AI 参考的 4-6 个原文切片，做到有据可查。

### 4. 📚 长文档切片 (Chunking)
支持 70+ 页的长篇 Paper（如 Llama 2 技术报告）。
- **技术实现**：使用 `RecursiveCharacterTextSplitter` 进行智能语义切分。

---

## 🛠️ 技术栈 (Tech Stack)

- **大模型 (LLM)**: DeepSeek Chat (V3)
- **开发框架**: LangChain Community
- **前端界面**: Streamlit
- **向量数据库**: ChromaDB (本地持久化)
- **文档处理**: PDFPlumber

---

## 💻 快速启动 (Quick Start)

如果你想在本地运行这个项目，请按以下步骤操作：

### 第一步：克隆项目
```bash
git clone [https://github.com/dwangbw-ops/rag.git](https://github.com/dwangbw-ops/rag.git)
cd rag
第二步：安装依赖
确保你的 Python 版本 >= 3.10
Bash
pip install -r requirements.txt
第三步：配置密钥
本项目使用 DeepSeek API。
在根目录新建一个文件名为 .env 的文件。
在里面填入你的 Key：
Plaintext
DEEPSEEK_API_KEY=sk-你的密钥粘贴在这里
第四步：启动应用
Bash
streamlit run app.py